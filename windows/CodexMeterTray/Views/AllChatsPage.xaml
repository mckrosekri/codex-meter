<?xml version="1.0" encoding="UTF-8" ?>
<Page
    x:Class="CodexMeterTray.Views.AllChatsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CodexMeterTray.Views"
    Background="{StaticResource AppBackgroundBrush}">
    <Grid Padding="32,24,32,32" RowSpacing="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Spacing="4">
                <TextBlock
                    FontSize="24"
                    FontWeight="SemiBold"
                    Foreground="{StaticResource AppTextBrush}"
                    Text="All Codex chats" />
                <TextBlock
                    x:Name="IndexSummaryText"
                    Foreground="{StaticResource AppMutedBrush}"
                    Text="Loading the native Codex index…" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                <ToggleButton
                    x:Name="ArchivedToggle"
                    AutomationProperties.Name="Show archived Codex chats"
                    Click="ArchivedToggle_Click"
                    Content="Archived" />
                <Button
                    AutomationProperties.Name="Refresh all Codex chats"
                    Click="RefreshButton_Click"
                    Content="Refresh" />
            </StackPanel>
        </Grid>

        <AutoSuggestBox
            x:Name="ChatSearchBox"
            Grid.Row="1"
            MaxWidth="720"
            HorizontalAlignment="Stretch"
            AutomationProperties.Name="Search all Codex chats"
            PlaceholderText="Search titles, folders, and projects"
            QueryIcon="Find"
            TextChanged="ChatSearchBox_TextChanged" />

        <Grid Grid.Row="2">
            <ListView
                x:Name="ChatList"
                AutomationProperties.Name="Indexed Codex chats"
                IsItemClickEnabled="True"
                ItemClick="ChatList_ItemClick"
                ItemsSource="{x:Bind Rows, Mode=OneWay}"
                SelectionMode="None">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Margin" Value="0,0,0,8" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:ChatIndexRow">
                        <Border
                            Padding="16,13"
                            Background="{StaticResource AppSurfaceBrush}"
                            BorderBrush="{StaticResource AppLineBrush}"
                            BorderThickness="1"
                            CornerRadius="10">
                            <Grid ColumnSpacing="18">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Spacing="4">
                                    <TextBlock
                                        FontSize="14"
                                        FontWeight="SemiBold"
                                        Foreground="{StaticResource AppTextBrush}"
                                        Text="{x:Bind Title}"
                                        TextTrimming="CharacterEllipsis" />
                                    <TextBlock
                                        Foreground="{StaticResource AppMutedBrush}"
                                        MaxLines="2"
                                        Text="{x:Bind Preview}"
                                        TextTrimming="CharacterEllipsis"
                                        TextWrapping="Wrap" />
                                    <TextBlock
                                        FontSize="11"
                                        Foreground="{StaticResource AppFaintBrush}"
                                        Text="{x:Bind Metadata}"
                                        TextTrimming="CharacterEllipsis" />
                                </StackPanel>
                                <StackPanel Grid.Column="1" HorizontalAlignment="Right" Spacing="5">
                                    <Grid HorizontalAlignment="Right" ColumnSpacing="8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <Border
                                            Padding="7,3"
                                            Background="{StaticResource AppBackgroundBrush}"
                                            CornerRadius="8">
                                            <TextBlock
                                                FontSize="10"
                                                Foreground="{StaticResource AppMutedBrush}"
                                                Text="{x:Bind Placement}" />
                                        </Border>
                                        <Border
                                            Grid.Column="1"
                                            Width="8"
                                            Height="8"
                                            VerticalAlignment="Center"
                                            AutomationProperties.Name="{x:Bind AttentionText}"
                                            Background="{x:Bind AttentionBrush}"
                                            CornerRadius="4"
                                            ToolTipService.ToolTip="{x:Bind AttentionText}"
                                            Visibility="{x:Bind AttentionVisibility}" />
                                    </Grid>
                                    <TextBlock
                                        HorizontalAlignment="Right"
                                        FontSize="10"
                                        Foreground="{StaticResource AppFaintBrush}"
                                        Text="{x:Bind Updated}" />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <StackPanel
                x:Name="EmptyState"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Spacing="8"
                Visibility="Collapsed">
                <TextBlock
                    HorizontalAlignment="Center"
                    FontSize="18"
                    FontWeight="SemiBold"
                    Text="No matching chats" />
                <TextBlock
                    Foreground="{StaticResource AppMutedBrush}"
                    Text="Try another search or refresh the native Codex index." />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
